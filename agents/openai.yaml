interface:
  display_name: "Reader’s Seat"
  short_description: "换到读者的位置，生成、重写与诊断专业易读的工作文档"
  default_prompt: "使用 $reader-seat 从读者视角重构这份材料，让目标读者更容易理解和行动，同时保留事实、范围与证据边界。除非我明确指定，否则沿用原文主要语言，并将完整成稿生成为自包含 HTML。"
policy:
  allow_implicit_invocation: true
